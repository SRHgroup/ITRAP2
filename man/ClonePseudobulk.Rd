% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auxiliary.R
\name{ClonePseudobulk}
\alias{ClonePseudobulk}
\title{Compute Average Expression for Pseudobulks Based on Metadata Grouping}
\usage{
ClonePseudobulk(
  object,
  assay = "pMHC",
  slot = "scale.data",
  clone_col = "clone_id"
)
}
\arguments{
\item{object}{A Seurat object containing the assay data to be analyzed.}

\item{assay}{The name of the assay to use for expression data (default: "RNA").}

\item{slot}{The slot containing the expression data ("data", "counts", or "scale.data").}

\item{clone_col}{The name of the metadata column used to define groups (default: "clone_id").}
}
\value{
A matrix containing the average expression values for each feature across the defined
pseudobulk groups. Columns are named after the unique identifiers found in the \code{clone_col} metadata column.
}
\description{
This function calculates the average expression for each feature across pseudobulk samples,
where pseudobulks are defined by groups in a specified metadata column of a Seurat object.
It preserves the original column names and ensures that the averages are computed exactly
according to the specified assay and slot without any unwanted alterations to column names.
}
\examples{
# Assuming 'seurat_obj' is a Seurat object with a metadata column named 'clone_id'
pseudobulk_matrix <- ClonePseudobulk(seurat_obj, assay = "RNA", slot = "data", clone_col = "clone_id")
# This will return a matrix with average expressions for each 'clone_id' group.

}
