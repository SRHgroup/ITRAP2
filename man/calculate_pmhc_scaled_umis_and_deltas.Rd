% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auxiliary.R
\name{calculate_pmhc_scaled_umis_and_deltas}
\alias{calculate_pmhc_scaled_umis_and_deltas}
\title{Compute per-clone pMHC scaled UMI values and delta-scores}
\usage{
calculate_pmhc_scaled_umis_and_deltas(
  object,
  assay = "pMHC",
  pmhc_class_col = "pMHC_classification",
  out_scaled_col = "pMHC_scaled_umis",
  out_delta_col = "pMHC_deltas",
  clone_col = "clone_id",
  verbose = TRUE
)
}
\arguments{
\item{object}{A Seurat object containing a pMHC assay. The metadata must
include \code{clone_id} and a column with pMHC tokens (default:
\code{"pMHC_classification"}). If present, \code{object@misc$pmhc} must contain
a mapping between human-readable pMHC names and feature \code{Barcode}s.}

\item{assay}{Character. Name of the assay storing pMHC data (default: \code{"pMHC"}).}

\item{pmhc_class_col}{Character. Metadata column specifying pMHC tokens
collapsed per clone (e.g. \code{"A0201_GVLDAWVRV:A0201_KVDDTFYYV"}).}

\item{out_scaled_col}{Character. Name of the metadata column to store
per-clone collapsed scaled-UMI values (default \code{"pMHC_scaled_umis"}).}

\item{out_delta_col}{Character. Name of the metadata column to store
per-clone delta-scores (default \code{"pMHC_deltas"}).}

\item{clone_col}{Character. Metadata column that uniquely identifies a clone
(default \code{"clone_id"}).}

\item{verbose}{Logical. Whether to display a progress bar (default \code{TRUE}).}
}
\value{
The input Seurat object with two updated metadata columns:
\itemize{
\item \code{out_scaled_col} (character): ":"-collapsed per-clone UMI means
\item \code{out_delta_col}  (character): ":"-collapsed per-clone delta-scores
}
}
\description{
This function aggregates pMHC signals per T-cell clone in a Seurat object.
For every clone, it computes:
}
\details{
\itemize{
\item \strong{scaled UMI expression}: mean per-feature values from the assay \verb{@data}
collapsed into a ":"-separated string in the order of pMHC tokens.
\item \strong{delta scores}: feature-wise positive-part scaled expression
from \verb{@scale.data}, normalized and background-subtracted using the mean
expression of all \emph{non-target} pMHC features.
}

The pMHC features belonging to each clone are extracted from a metadata
column such as \code{"pMHC_classification"}. If the metadata encodes multiple
pMHCs for a clone in a single string (e.g. \code{"A0201_GVLDAVWRV:A0201_KVDDTFYYV"}),
the function splits and maps each token to the true assay feature (Barcode)
using \code{object@misc$pmhc}.

The resulting per-clone collapsed values are written back into
\code{object@meta.data} under new columns defined by \code{out_scaled_col}
and \code{out_delta_col}. Any previous versions of these columns are removed.

\strong{Returned columns}
\itemize{
\item \code{out_scaled_col}: e.g. \code{"0.124:0.522:0.991"}
\item \code{out_delta_col}:  e.g. \code{"0.001:0.551:-0.110"}
}

If a clone has no valid pMHC tokens or no mapped features,
\code{"Negative"} is returned for both fields.
}
\examples{
\dontrun{
  seurat_obj <- calculate_pmhc_scaled_umis_and_deltas(seurat_obj)
  head(seurat_obj@meta.data[c("pMHC_scaled_umis", "pMHC_deltas")])
}

}
