% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rosner.R
\name{extreme_outlier_test}
\alias{extreme_outlier_test}
\title{Detect Outliers Using Extreme Value Theory}
\usage{
extreme_outlier_test(
  x,
  type = "regular",
  double_loc_scale = F,
  extreme_alpha = 0.001
)
}
\arguments{
\item{x}{Numeric vector of values to test for extreme outliers.}

\item{type}{Character. Method to estimate extreme values. Options:
\itemize{
\item \code{'regular'} (default): Uses the estimated Gumbel distribution parameters.
\item \code{'reestimate_params'}: Iteratively re-estimates the distribution parameters, removing extreme values one at a time.
\item \code{'extreme_without_tail'}: Estimates distribution parameters after removing the three highest values.
}}

\item{double_loc_scale}{Logical. If \code{TRUE}, doubles the fitted location and scale parameters before calculating p-values for \code{type = "regular"}.}

\item{extreme_alpha}{Numeric significance threshold used when \code{type = "reestimate_params"}.}
}
\value{
A numeric vector of p-values for each observation in \code{x}. Lower values indicate a higher likelihood of being an outlier.
If \code{reestimate_params} or \code{extreme_without_tail} is used, the function may return slightly different p-values based on the refined distribution.
}
\description{
This function applies an Extreme Value Theory (EVT) approach using the Gumbel distribution to detect outliers.
It estimates the distribution parameters and calculates p-values for each observation, based on different extreme
outlier detection strategies.
}
\examples{
set.seed(123)
x <- c(rnorm(100, mean=50, sd=10), 120, 130, 140)  # Simulated data with outliers
pvals <- extreme_outlier_test(x, type="regular")
print(pvals)

}
