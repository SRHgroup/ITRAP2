% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auxiliary.R
\name{filter_mismatch_responses}
\alias{filter_mismatch_responses}
\title{Filter pMHC–TCR pairs by donor HLA matching and collapse matched pMHC features per clone}
\usage{
filter_mismatch_responses(object, keep_unfiltered = T, donor_col = "donor")
}
\arguments{
\item{object}{Seurat object containing pMHC–TCR metadata in \verb{@meta.data} and
HLA genotypes in \verb{@misc$hla_genotype}. The metadata must contain
\code{clone_id}, \code{HLA}, and pMHC fields.}

\item{keep_unfiltered}{Logical; if \code{TRUE} (default), the function stores the
original pMHC metadata columns in new columns ending with \verb{_unf} before
filtering.}

\item{donor_col}{Name of the metadata column identifying the donor for each
cell. Must match the first column name of the HLA genotype table stored in
\code{object@misc$hla_genotype}.}
}
\value{
A Seurat object with updated \verb{@meta.data}, where:
\itemize{
\item HLA-matching pMHC–TCR pairs are retained,
\item pMHC metadata are collapsed per \code{clone_id},
\item optional unfiltered metadata columns are added with \verb{_unf} suffix.
}
}
\description{
This function takes a Seurat object containing TCR–pMHC assignments and donor
HLA genotypes (stored in \code{object@misc$hla_genotype}). For each cell, it checks
whether the presented pMHC allele matches one of the donor's typed HLA alleles.
Only HLA-matched pMHC–TCR pairs are retained, and pMHC-related metadata
(\code{pMHC_classification}, confidence scores, deltas, UMIs, etc.) are collapsed
per clone into a single ":"-separated string.
}
\details{
Optionally, the function preserves the unfiltered pMHC metadata columns before
filtering and collapsing, allowing downstream comparison between filtered and
unfiltered data.
}
\examples{
\dontrun{
  seurat_filtered <- filter_mismatch_responses(seurat_obj, donor_col = "donor")
  head(seurat_filtered@meta.data)
}

}
