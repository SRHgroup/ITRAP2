% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_and_smooth.R
\name{adjust_pmhc_pvalues}
\alias{adjust_pmhc_pvalues}
\title{Adjust pMHC P-values Within Clones}
\usage{
adjust_pmhc_pvalues(
  df,
  adjust_permutation,
  adjust_test_within_clone,
  method = "BH"
)
}
\arguments{
\item{df}{A data frame containing \code{pMHC_pvalues} and \code{pMHC_wclone_pvalues} as \code{:}-separated strings.}

\item{adjust_permutation}{Logical. If \code{TRUE}, adjust \code{pMHC_pvalues} using \code{p.adjust()}.}

\item{adjust_test_within_clone}{Logical. If \code{TRUE}, adjust \code{pMHC_wclone_pvalues} using \code{p.adjust()}.}

\item{method}{Character string specifying the multiple testing correction method for \code{p.adjust()}. Default is \code{"BH"}.}
}
\value{
A modified data frame with adjusted p-values, where each \code{pMHC_pvalues} and \code{pMHC_wclone_pvalues}
are re-collapsed back into \code{:}-separated format after adjustment.
}
\description{
This function processes a data frame containing pMHC classifications, confidence scores, and p-values,
separating multiple p-values per row and adjusting them using \code{p.adjust()}. The results are re-collapsed
into the original format, keeping clone-level grouping.
}
\details{
The function first splits the \code{pMHC_pvalues} and \code{pMHC_wclone_pvalues} columns into separate rows,
applies \code{p.adjust()} per \code{clone_id}, and then re-aggregates them back into their original structure.
}
\examples{
df_adjusted <- adjust_pmhc_pvalues(df, adjust_permutation = TRUE, adjust_test_within_clone = TRUE)

}
